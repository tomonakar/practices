# Deep Copyの実装

構造体に`DeepCopy`メソッドをもたせ、Shallow Copyの課題を解決する

- `Shallow Copy`では参照が共有されるため、意図しないデータ変更が発生する
- 各構造体に DeepCopy メソッドを追加することで、安全な深いコピーが可能になる
- ポインタやスライスなど、参照型データは明示的にコピーする必要がある
- ただし、すべての構造体に DeepCopy を実装するのは手間がかかるため、別の方法も検討する余地がある
