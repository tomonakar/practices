# Shallow Copy と Deep Copy

## 問題点
- shallow copyではポインタの参照先が共有されてしまうため、オブジェクトの値が意図せず変わってしまう
- オブジェクトが参照している全ての要素も含めて新しいオブジェクトとして作成し、その内容をコピーすればコピー後のオブジェクトをは元のオブジェクトの参照を持たない（Deep Copy）
- オブジェクトの入れ子構造が深くなると手動でのコピーが煩雑になるため、プロトタイプパターンを利用する（他にはシリアライズ・デシリアライズを利用するなども考えられる）